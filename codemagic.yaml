workflows:
  android-apk:
    name: Android APK

    environment:
      python: 3.11

    scripts:
      - pip install --upgrade pip
      - pip install buildozer cython
      - buildozer android debug

    artifacts:
      - bin/*.apk
